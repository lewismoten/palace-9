# Dependencies and local runtime state
node_modules/
__pycache__/
*.py[cod]
*.log
*.pid

# Secrets, credentials, and machine-specific configuration
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
*.crt
*.cer
*.kdbx
credentials*
secrets*
.ssh/

# Editor, OS, and test runtime noise
.DS_Store
Thumbs.db
.idea/
.vscode/
.pytest_cache/
.coverage
htmlcov/

# Local services, generated reports, and temporary test outputs
data/*.tmp
.tmp-*/
snapshots/
cuda-snapshots/
quantization-results.json

# Training archives are reproducible local artifacts; only the promoted
# browser checkpoint in data/ is versioned.
torch-snapshots-*/
torch-snapshots*/

# Locally built CUDA experiments
cuda_moe_train
cuda_probe

# Generated deployment checkpoints and duplicate local exports. The authoritative
# curated model package is versioned under release/palace-9-local-v1/.
/palace9-qwen2moe-*/
/palace9-qwen2moe-*.gguf
/Modelfile.preview-*
/readme.md
/weight-inspector.jpg
